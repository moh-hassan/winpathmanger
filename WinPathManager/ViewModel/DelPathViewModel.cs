using System;
using System.Collections.Generic;
using System.IO;
using System.Windows.Forms;
using BusinessObjects;
using Microsoft.Win32;
using MvvmFx.Windows.Input;
using NLog;

namespace WinPathManager.ViewModel
{
    public class DelPathViewModel : BusinessObject
    {
        private static Logger _logger = LogManager.GetCurrentClassLogger();
       // public BoundCommand RemoveCommand;
        
        /*
        public DelPathViewModel()
        {

           // RemoveCommand = new BoundCommand(RemoveAction, CanRemove, null);
        }
        

        private bool CanRemove(object o)
        {
            //  MessageBox.Show("canbrowse:" + SelectionMode .ToString( ));
            return false ;
        }

        void RemoveAction(object obj)
        {
        
        }

       */

    }//
}
